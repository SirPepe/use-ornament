{
  "name": "use-ornament",
  "version": "1.0.0",
  "description": "Website for @sirpepe/ornament",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate.js && vite src",
    "build": "rm -rf docs && mkdir docs && cp static/.htaccess docs/.htaccess && node scripts/generate.js && vite build src --base=./ --out-dir=../docs"
  },
  "author": "peter@peterkroener.de",
  "license": "MIT",
  "devDependencies": {
    "@codemovie/code-movie": "^0.0.38",
    "@codemovie/code-movie-runtime": "^2.0.1",
    "@sirpepe/ornament": "^3.0.0",
    "marked": "^17.0.0",
    "marked-gfm-heading-id": "^4.0.0",
    "vite": "^7.3.0"
  },
  "volta": {
    "node": "24.12.0"
  }
}
